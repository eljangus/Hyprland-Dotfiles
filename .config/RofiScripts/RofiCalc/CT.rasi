@import "~/.config/colors/roficolors.rasi"
@import "~/.config/colors/rofiradius.rasi"

window {
  width: 900px;
  padding: 0;
  border: 2;
  border-color: var(border);
  border-radius: var(borderradius);
  transparency: "real";
  background-color: #00000000;
}

error-message {
  enabled: true;
  padding: 40px;
  background-color: var(background);
  border-color: var(border);
  border-radius: var(borderradius);
}

message {
  enabled: true;
  padding: 0 0 0 13;
  margin: 0px 0px 0px 0px;
  border: 0px;
  background-color: var(backgroundD);
  border-color: var(border);
  border-radius: 0px;
}

textbox {
  enabled: true;
  padding: 12;
  str: "Keine Ergebnisse!";
  text-color: var(textcolor);
  background-color: transparent;
  border: 0px;
  border-radius: var(borderradiuselement);
  border-color: var(border);
}

textbox-custom {
  enabled: true;
  padding: 20 10 20 30;
  margin: 0 -3 0 -3;
  str: "󰃬";
  expand: false;
  text-color: var(textcolor);
  background-color: var(backgroundD);
  border: 0px;
  border-radius: 0px;
  border-color: var(border);
}

entry {
  placeholder: "Rechnen...";
  placeholder-color: var(searchcolor);
  text-color: var(textcolor);
  border: 0;
  border-color: var(border);
  padding: 20 30 20 10;
  border-radius: 0px;
  background-color: var(backgroundD);
}

listview {
  margin: 0 0 0 0;
  padding: 0;
  columns: 1;
  background-color: var(background);
  scrollbar: false;
  cycle: false;
  border: 0;
  lines: 8;
  border-color: var(border);
}

mainbox {
  background-color: var(background);
  spacing: 0; 
  padding: 0;
  border-radius: var(borderradius);
  children: [ inputbar, message, listview ];
}

inputbar {
  padding: 0;
  background-color: var(background);
  children: [ textbox-custom , entry ];
  border: 0;
  border-color: var(border);
}

prompt{
 enabled: true;
 padding: 12 28 12 19;
 margin: 0 30 0 0;
 text-color: var(textcolor);
 border: 2;
 border-color: var(border);
 border-radius: 20;
 background-color: var(background);
}

element{
 padding: 15 23 15 23;
 margin: 0;
 border-radius: 0px;
 border: 0;
}

element normal.normal{
 background-color: transparent;
 text-color: var(textcolor);
}

element normal.active{
 background-color: transparent;
 text-color: var(textcolor);
}

element normal.urgent{
 background-color: transparent;
 text-color: var(textcolor);
}

element alternate.active{
 background-color: transparent;
 text-color: var(textcolor);
}

element alternate.normal{
 background-color: transparent;
 text-color: var(textcolor);
}

element alternate.urgent{
 background-color: transparent;
 text-color: var(textcolor);
}

element-text{
 background-color: #00000000;
 text-color: inherit;
 padding: 0;
 vertical-align: 0.5;
 highlight: bold underline;
}

element selected.active,
element selected.urgent,
element selected.normal {
 background-color: var(selection);
 text-color: var(background);
 border: 0;
 border-color: var(selectionborder);
}

element-icon{
 size: 48;
 background-color: transparent;
 text-color: inherit;
}

mode-switcher{
 padding: 0;
 background-color: #00000000;
  margin: 0 20 0 20;
 children: [ button ];
}

button{
 padding: 0 26 0 21;
 margin: 0 10 0 10;
 border: 2;
 border-radius: 20;
 border-color: var(border);
 background-color: var(background);
 text-color: var(textcolor);
}


button selected.normal {
 background-color: var(background);
 text-color: var(accent2);
}
